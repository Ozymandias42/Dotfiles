#zsh profile. 

#This file is intended as list of actions to be taken when initialising a login shell.
#Consequently this means, that the actions defined herein are to be done ONCE 
#as opposed to every time a new interactive shell is started in bashrc

#Parsing of general .profile for all POSIX compliant shells.
#[[ -e ~/.profile ]] && emulate sh -c 'source ~/.profile'
